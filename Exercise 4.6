def computepay(h,r):
    if h>40:
        p=1.5*r*(h-40)+(40*r)
    else:
        p=h*r
    return p

hrs = input("Enter Hours:")
hr= float(hrs)
r= input("Enter rate per hour:")
rph= float(r)
p = computepay(hr,rph)
print("Pay",p)
